package org.example.semafori;

import java.util.Scanner;

public class Semafori {
    public void semafori(){
        Scanner scanner = new Scanner(System.in);
        String[] inputs = scanner.nextLine().split(" ");

        int

        int totalDistance = Integer.parseInt(inputs[1]);

        int currentSecondsTravelled = 0;

        for(int i = 0)
        String[] trafficLight = scanner.nextLine().split(" ");

    }
}
